// Test stub: the real 'server-only' package throws unless it is loaded inside a
// React Server Component. That guard is exactly what we want in the app, so it is
// swapped for this no-op only under vitest.
export {}
